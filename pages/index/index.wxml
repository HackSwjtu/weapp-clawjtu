<scroll-view id="post-container" scroll-y="true" enable-back-to-top="true">
    <view class="post" wx:for="{{posts}}" wx:for-item="post" wx:key="null">
        <navigator url="../speech/speech?id={{post.id}}">
            <view class="post-info">
                <text class="post-title">{{post.title}}</text>
                <view class="post-description" wx:for="{{post.descriptions}}" wx:for-item="description" wx:key="null">
                    <text>{{description}}</text>
                </view>
            </view>
            <view class="hr"></view>
            <view class="post-preview">{{post.content}}</view>
        </navigator>
        <image id="fav-{{post.id}}" class="icon post-love {{favs[post.id] ? 'loved' : 'unloved'}}" catchtap="tapFavIcon" src="../../images/love.png" mode="aspectFit"></image>
    </view>
</scroll-view>
<view id="banner">
    <image id="avatar" src="{{userInfo.avatarUrl}}" mode="aspectFit"></image>
    <text id="nickname">{{userInfo.nickName}}</text>
    <view style="flex-grow: 1"></view>
    <navigator url="../favorite/favorite" id="favorite"><image class="icon" src="../../images/favorite.png" mode="aspectFit"></image></navigator>
</view>
<view id="tablet" class="w100p">
    <navigator class="tablet" open-type="redirectTo" url="index">
        <image src="../../images/speech.png" class="icon"></image>
    </navigator>
    <view class="tablet" bindtap="tapCompetition">
        <image src="../../images/trophy.png" class="icon active"></image>
    </view>
</view>